import * as actionTypes from '../actionTypes/actionTypes'

export function rootReducer(state = {},action) {
        switch (action.type) {
            case actionTypes.ON_LIGHTER : {
                return ({
                })
            }

        }
        return state;
}

